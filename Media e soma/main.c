#include <stdio.h>

int main(void) {
  // int idade = 0;
  // printf("Informe sua Idade:");
  // scanf("%d", &idade);

  // printf("Sua idade é: %d", idade);

  // int anos = 0;
  // int meses = 0;
  // int dias = 0;
  // int total = 0;

  // printf("Digite os anos: ");
  // scanf("%d", &anos);
  // printf("Digite os meses: ");
  // scanf("%d", &meses);
  // printf("Digite os dias: ");
  // scanf("%d", &dias);

  // total = anos * 365 + meses * 30 + dias;

  // printf("total vivido: %d", total);
  

  // int qtdMinima = 0;
  // int qtdMaxima = 0;
  // float media = 0;

 // printf("Digite a qtd mínima:");
  // scanf("%i", &qtdMinima);
  // printf("Digite a qtd máxima");
  // scanf("%i", &qtdMaxima);

  // media = (qtdMinima + qtdMaxima) / 2;

    // printf("media: %.1f", media);
  

  // int estoqueMax = 0;
  // int estoqueMin = 0;
  // float media = 0;

  // printf("Estoque Mínimo?");
  // scanf("%d", &estoqueMin);
  // printf("Estoque Máximo?");
  // scanf("%d", &estoqueMax);

  // media = (estoqueMax + estoqueMin) / 2;

  // printf("A média é: %.2f", media);

  // int a = 5;
  // int b = 0;
  // a = --b;
  // a = b--;
  // printf("%d", a);  

  
  return 0;
}